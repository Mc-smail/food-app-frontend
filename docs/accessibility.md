# Accessibility Notes

Small static sites should keep navigation and content easy to use.

Recommended checks:

- every page has a descriptive title
- links use meaningful labels
- text contrast stays readable
- navigation is keyboard-friendly
- contact links use correct `tel:` and `mailto:` formats
